import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Layout } from './index';

<Meta title="MDX/Layout" component={Layout} />

export const Template = args => <Layout {...args} />;

# Layout

With `MDX`, we can define a story for `Layout` right in the middle of our Markdown documentation.

<Canvas>
  <Story
    name="Normal"
    args={{
      color:''
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>