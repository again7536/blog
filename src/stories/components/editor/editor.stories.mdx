import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { MarkdownEditor } from './index';

<Meta title="Components/Editor" component={MarkdownEditor} />

export const Template = args => <MarkdownEditor {...args} />;

# Editor

With `MDX`, we can define a story for `Editor` right in the middle of our Markdown documentation.

<Canvas>
  <Story
    name="Normal"
    args={{
      color:''
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>